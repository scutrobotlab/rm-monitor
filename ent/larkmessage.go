// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"scutbot.cn/web/rm-monitor/ent/larkmessage"
	"scutbot.cn/web/rm-monitor/ent/match"
)

// LarkMessage is the model entity for the LarkMessage schema.
type LarkMessage struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// MessageID holds the value of the "message_id" field.
	MessageID string `json:"message_id,omitempty"`
	// CardID holds the value of the "card_id" field.
	CardID *string `json:"card_id,omitempty"`
	// CardPayload holds the value of the "card_payload" field.
	CardPayload map[string]interface{} `json:"card_payload,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the LarkMessageQuery when eager-loading is set.
	Edges               LarkMessageEdges `json:"edges"`
	match_lark_messages *string
	selectValues        sql.SelectValues
}

// LarkMessageEdges holds the relations/edges for other nodes in the graph.
type LarkMessageEdges struct {
	// Match holds the value of the match edge.
	Match *Match `json:"match,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// MatchOrErr returns the Match value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e LarkMessageEdges) MatchOrErr() (*Match, error) {
	if e.Match != nil {
		return e.Match, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: match.Label}
	}
	return nil, &NotLoadedError{edge: "match"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LarkMessage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case larkmessage.FieldCardPayload:
			values[i] = new([]byte)
		case larkmessage.FieldID:
			values[i] = new(sql.NullInt64)
		case larkmessage.FieldMessageID, larkmessage.FieldCardID:
			values[i] = new(sql.NullString)
		case larkmessage.FieldCreatedAt, larkmessage.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case larkmessage.ForeignKeys[0]: // match_lark_messages
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LarkMessage fields.
func (_m *LarkMessage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case larkmessage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case larkmessage.FieldMessageID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field message_id", values[i])
			} else if value.Valid {
				_m.MessageID = value.String
			}
		case larkmessage.FieldCardID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field card_id", values[i])
			} else if value.Valid {
				_m.CardID = new(string)
				*_m.CardID = value.String
			}
		case larkmessage.FieldCardPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field card_payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.CardPayload); err != nil {
					return fmt.Errorf("unmarshal field card_payload: %w", err)
				}
			}
		case larkmessage.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case larkmessage.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case larkmessage.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field match_lark_messages", values[i])
			} else if value.Valid {
				_m.match_lark_messages = new(string)
				*_m.match_lark_messages = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LarkMessage.
// This includes values selected through modifiers, order, etc.
func (_m *LarkMessage) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryMatch queries the "match" edge of the LarkMessage entity.
func (_m *LarkMessage) QueryMatch() *MatchQuery {
	return NewLarkMessageClient(_m.config).QueryMatch(_m)
}

// Update returns a builder for updating this LarkMessage.
// Note that you need to call LarkMessage.Unwrap() before calling this method if this LarkMessage
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *LarkMessage) Update() *LarkMessageUpdateOne {
	return NewLarkMessageClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the LarkMessage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *LarkMessage) Unwrap() *LarkMessage {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: LarkMessage is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *LarkMessage) String() string {
	var builder strings.Builder
	builder.WriteString("LarkMessage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("message_id=")
	builder.WriteString(_m.MessageID)
	builder.WriteString(", ")
	if v := _m.CardID; v != nil {
		builder.WriteString("card_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("card_payload=")
	builder.WriteString(fmt.Sprintf("%v", _m.CardPayload))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// LarkMessages is a parsable slice of LarkMessage.
type LarkMessages []*LarkMessage
