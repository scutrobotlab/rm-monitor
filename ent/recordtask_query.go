// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"scutbot.cn/web/rm-monitor/ent/matchround"
	"scutbot.cn/web/rm-monitor/ent/mediaartifact"
	"scutbot.cn/web/rm-monitor/ent/predicate"
	"scutbot.cn/web/rm-monitor/ent/recordtask"
	"scutbot.cn/web/rm-monitor/ent/uploadtask"
)

// RecordTaskQuery is the builder for querying RecordTask entities.
type RecordTaskQuery struct {
	config
	ctx                *QueryContext
	order              []recordtask.OrderOption
	inters             []Interceptor
	predicates         []predicate.RecordTask
	withMatchRound     *MatchRoundQuery
	withUploadTask     *UploadTaskQuery
	withMediaArtifacts *MediaArtifactQuery
	withFKs            bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RecordTaskQuery builder.
func (_q *RecordTaskQuery) Where(ps ...predicate.RecordTask) *RecordTaskQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *RecordTaskQuery) Limit(limit int) *RecordTaskQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *RecordTaskQuery) Offset(offset int) *RecordTaskQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *RecordTaskQuery) Unique(unique bool) *RecordTaskQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *RecordTaskQuery) Order(o ...recordtask.OrderOption) *RecordTaskQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryMatchRound chains the current query on the "match_round" edge.
func (_q *RecordTaskQuery) QueryMatchRound() *MatchRoundQuery {
	query := (&MatchRoundClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(recordtask.Table, recordtask.FieldID, selector),
			sqlgraph.To(matchround.Table, matchround.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, recordtask.MatchRoundTable, recordtask.MatchRoundColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUploadTask chains the current query on the "upload_task" edge.
func (_q *RecordTaskQuery) QueryUploadTask() *UploadTaskQuery {
	query := (&UploadTaskClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(recordtask.Table, recordtask.FieldID, selector),
			sqlgraph.To(uploadtask.Table, uploadtask.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, recordtask.UploadTaskTable, recordtask.UploadTaskColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMediaArtifacts chains the current query on the "media_artifacts" edge.
func (_q *RecordTaskQuery) QueryMediaArtifacts() *MediaArtifactQuery {
	query := (&MediaArtifactClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(recordtask.Table, recordtask.FieldID, selector),
			sqlgraph.To(mediaartifact.Table, mediaartifact.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, recordtask.MediaArtifactsTable, recordtask.MediaArtifactsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first RecordTask entity from the query.
// Returns a *NotFoundError when no RecordTask was found.
func (_q *RecordTaskQuery) First(ctx context.Context) (*RecordTask, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{recordtask.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *RecordTaskQuery) FirstX(ctx context.Context) *RecordTask {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RecordTask ID from the query.
// Returns a *NotFoundError when no RecordTask ID was found.
func (_q *RecordTaskQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{recordtask.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *RecordTaskQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RecordTask entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RecordTask entity is found.
// Returns a *NotFoundError when no RecordTask entities are found.
func (_q *RecordTaskQuery) Only(ctx context.Context) (*RecordTask, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{recordtask.Label}
	default:
		return nil, &NotSingularError{recordtask.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *RecordTaskQuery) OnlyX(ctx context.Context) *RecordTask {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RecordTask ID in the query.
// Returns a *NotSingularError when more than one RecordTask ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *RecordTaskQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{recordtask.Label}
	default:
		err = &NotSingularError{recordtask.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *RecordTaskQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RecordTasks.
func (_q *RecordTaskQuery) All(ctx context.Context) ([]*RecordTask, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RecordTask, *RecordTaskQuery]()
	return withInterceptors[[]*RecordTask](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *RecordTaskQuery) AllX(ctx context.Context) []*RecordTask {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RecordTask IDs.
func (_q *RecordTaskQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(recordtask.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *RecordTaskQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *RecordTaskQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*RecordTaskQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *RecordTaskQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *RecordTaskQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *RecordTaskQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RecordTaskQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *RecordTaskQuery) Clone() *RecordTaskQuery {
	if _q == nil {
		return nil
	}
	return &RecordTaskQuery{
		config:             _q.config,
		ctx:                _q.ctx.Clone(),
		order:              append([]recordtask.OrderOption{}, _q.order...),
		inters:             append([]Interceptor{}, _q.inters...),
		predicates:         append([]predicate.RecordTask{}, _q.predicates...),
		withMatchRound:     _q.withMatchRound.Clone(),
		withUploadTask:     _q.withUploadTask.Clone(),
		withMediaArtifacts: _q.withMediaArtifacts.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithMatchRound tells the query-builder to eager-load the nodes that are connected to
// the "match_round" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RecordTaskQuery) WithMatchRound(opts ...func(*MatchRoundQuery)) *RecordTaskQuery {
	query := (&MatchRoundClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMatchRound = query
	return _q
}

// WithUploadTask tells the query-builder to eager-load the nodes that are connected to
// the "upload_task" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RecordTaskQuery) WithUploadTask(opts ...func(*UploadTaskQuery)) *RecordTaskQuery {
	query := (&UploadTaskClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUploadTask = query
	return _q
}

// WithMediaArtifacts tells the query-builder to eager-load the nodes that are connected to
// the "media_artifacts" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *RecordTaskQuery) WithMediaArtifacts(opts ...func(*MediaArtifactQuery)) *RecordTaskQuery {
	query := (&MediaArtifactClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withMediaArtifacts = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Role string `json:"role,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RecordTask.Query().
//		GroupBy(recordtask.FieldRole).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *RecordTaskQuery) GroupBy(field string, fields ...string) *RecordTaskGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RecordTaskGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = recordtask.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Role string `json:"role,omitempty"`
//	}
//
//	client.RecordTask.Query().
//		Select(recordtask.FieldRole).
//		Scan(ctx, &v)
func (_q *RecordTaskQuery) Select(fields ...string) *RecordTaskSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &RecordTaskSelect{RecordTaskQuery: _q}
	sbuild.label = recordtask.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RecordTaskSelect configured with the given aggregations.
func (_q *RecordTaskQuery) Aggregate(fns ...AggregateFunc) *RecordTaskSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *RecordTaskQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !recordtask.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *RecordTaskQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RecordTask, error) {
	var (
		nodes       = []*RecordTask{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withMatchRound != nil,
			_q.withUploadTask != nil,
			_q.withMediaArtifacts != nil,
		}
	)
	if _q.withMatchRound != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, recordtask.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RecordTask).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RecordTask{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withMatchRound; query != nil {
		if err := _q.loadMatchRound(ctx, query, nodes, nil,
			func(n *RecordTask, e *MatchRound) { n.Edges.MatchRound = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUploadTask; query != nil {
		if err := _q.loadUploadTask(ctx, query, nodes, nil,
			func(n *RecordTask, e *UploadTask) { n.Edges.UploadTask = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withMediaArtifacts; query != nil {
		if err := _q.loadMediaArtifacts(ctx, query, nodes,
			func(n *RecordTask) { n.Edges.MediaArtifacts = []*MediaArtifact{} },
			func(n *RecordTask, e *MediaArtifact) { n.Edges.MediaArtifacts = append(n.Edges.MediaArtifacts, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *RecordTaskQuery) loadMatchRound(ctx context.Context, query *MatchRoundQuery, nodes []*RecordTask, init func(*RecordTask), assign func(*RecordTask, *MatchRound)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*RecordTask)
	for i := range nodes {
		if nodes[i].match_round_record_tasks == nil {
			continue
		}
		fk := *nodes[i].match_round_record_tasks
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(matchround.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "match_round_record_tasks" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *RecordTaskQuery) loadUploadTask(ctx context.Context, query *UploadTaskQuery, nodes []*RecordTask, init func(*RecordTask), assign func(*RecordTask, *UploadTask)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*RecordTask)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.UploadTask(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(recordtask.UploadTaskColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.record_task_upload_task
		if fk == nil {
			return fmt.Errorf(`foreign-key "record_task_upload_task" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "record_task_upload_task" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *RecordTaskQuery) loadMediaArtifacts(ctx context.Context, query *MediaArtifactQuery, nodes []*RecordTask, init func(*RecordTask), assign func(*RecordTask, *MediaArtifact)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*RecordTask)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.MediaArtifact(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(recordtask.MediaArtifactsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.record_task_media_artifacts
		if fk == nil {
			return fmt.Errorf(`foreign-key "record_task_media_artifacts" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "record_task_media_artifacts" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *RecordTaskQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *RecordTaskQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(recordtask.Table, recordtask.Columns, sqlgraph.NewFieldSpec(recordtask.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, recordtask.FieldID)
		for i := range fields {
			if fields[i] != recordtask.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *RecordTaskQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(recordtask.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = recordtask.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RecordTaskGroupBy is the group-by builder for RecordTask entities.
type RecordTaskGroupBy struct {
	selector
	build *RecordTaskQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *RecordTaskGroupBy) Aggregate(fns ...AggregateFunc) *RecordTaskGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *RecordTaskGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecordTaskQuery, *RecordTaskGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *RecordTaskGroupBy) sqlScan(ctx context.Context, root *RecordTaskQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RecordTaskSelect is the builder for selecting fields of RecordTask entities.
type RecordTaskSelect struct {
	*RecordTaskQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *RecordTaskSelect) Aggregate(fns ...AggregateFunc) *RecordTaskSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *RecordTaskSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RecordTaskQuery, *RecordTaskSelect](ctx, _s.RecordTaskQuery, _s, _s.inters, v)
}

func (_s *RecordTaskSelect) sqlScan(ctx context.Context, root *RecordTaskQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
