// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"scutbot.cn/web/rm-monitor/ent/mediaartifact"
	"scutbot.cn/web/rm-monitor/ent/predicate"
	"scutbot.cn/web/rm-monitor/ent/transcodetask"
)

// TranscodeTaskQuery is the builder for querying TranscodeTask entities.
type TranscodeTaskQuery struct {
	config
	ctx                 *QueryContext
	order               []transcodetask.OrderOption
	inters              []Interceptor
	predicates          []predicate.TranscodeTask
	withSourceArtifact  *MediaArtifactQuery
	withArchiveArtifact *MediaArtifactQuery
	withFKs             bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TranscodeTaskQuery builder.
func (_q *TranscodeTaskQuery) Where(ps ...predicate.TranscodeTask) *TranscodeTaskQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *TranscodeTaskQuery) Limit(limit int) *TranscodeTaskQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *TranscodeTaskQuery) Offset(offset int) *TranscodeTaskQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *TranscodeTaskQuery) Unique(unique bool) *TranscodeTaskQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *TranscodeTaskQuery) Order(o ...transcodetask.OrderOption) *TranscodeTaskQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QuerySourceArtifact chains the current query on the "source_artifact" edge.
func (_q *TranscodeTaskQuery) QuerySourceArtifact() *MediaArtifactQuery {
	query := (&MediaArtifactClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(transcodetask.Table, transcodetask.FieldID, selector),
			sqlgraph.To(mediaartifact.Table, mediaartifact.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, transcodetask.SourceArtifactTable, transcodetask.SourceArtifactColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryArchiveArtifact chains the current query on the "archive_artifact" edge.
func (_q *TranscodeTaskQuery) QueryArchiveArtifact() *MediaArtifactQuery {
	query := (&MediaArtifactClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(transcodetask.Table, transcodetask.FieldID, selector),
			sqlgraph.To(mediaartifact.Table, mediaartifact.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, true, transcodetask.ArchiveArtifactTable, transcodetask.ArchiveArtifactColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TranscodeTask entity from the query.
// Returns a *NotFoundError when no TranscodeTask was found.
func (_q *TranscodeTaskQuery) First(ctx context.Context) (*TranscodeTask, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{transcodetask.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *TranscodeTaskQuery) FirstX(ctx context.Context) *TranscodeTask {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TranscodeTask ID from the query.
// Returns a *NotFoundError when no TranscodeTask ID was found.
func (_q *TranscodeTaskQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{transcodetask.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *TranscodeTaskQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TranscodeTask entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TranscodeTask entity is found.
// Returns a *NotFoundError when no TranscodeTask entities are found.
func (_q *TranscodeTaskQuery) Only(ctx context.Context) (*TranscodeTask, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{transcodetask.Label}
	default:
		return nil, &NotSingularError{transcodetask.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *TranscodeTaskQuery) OnlyX(ctx context.Context) *TranscodeTask {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TranscodeTask ID in the query.
// Returns a *NotSingularError when more than one TranscodeTask ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *TranscodeTaskQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{transcodetask.Label}
	default:
		err = &NotSingularError{transcodetask.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *TranscodeTaskQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TranscodeTasks.
func (_q *TranscodeTaskQuery) All(ctx context.Context) ([]*TranscodeTask, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TranscodeTask, *TranscodeTaskQuery]()
	return withInterceptors[[]*TranscodeTask](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *TranscodeTaskQuery) AllX(ctx context.Context) []*TranscodeTask {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TranscodeTask IDs.
func (_q *TranscodeTaskQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(transcodetask.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *TranscodeTaskQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *TranscodeTaskQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*TranscodeTaskQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *TranscodeTaskQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *TranscodeTaskQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *TranscodeTaskQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TranscodeTaskQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *TranscodeTaskQuery) Clone() *TranscodeTaskQuery {
	if _q == nil {
		return nil
	}
	return &TranscodeTaskQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]transcodetask.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.TranscodeTask{}, _q.predicates...),
		withSourceArtifact:  _q.withSourceArtifact.Clone(),
		withArchiveArtifact: _q.withArchiveArtifact.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithSourceArtifact tells the query-builder to eager-load the nodes that are connected to
// the "source_artifact" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TranscodeTaskQuery) WithSourceArtifact(opts ...func(*MediaArtifactQuery)) *TranscodeTaskQuery {
	query := (&MediaArtifactClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSourceArtifact = query
	return _q
}

// WithArchiveArtifact tells the query-builder to eager-load the nodes that are connected to
// the "archive_artifact" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *TranscodeTaskQuery) WithArchiveArtifact(opts ...func(*MediaArtifactQuery)) *TranscodeTaskQuery {
	query := (&MediaArtifactClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withArchiveArtifact = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Status transcodetask.Status `json:"status,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TranscodeTask.Query().
//		GroupBy(transcodetask.FieldStatus).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *TranscodeTaskQuery) GroupBy(field string, fields ...string) *TranscodeTaskGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TranscodeTaskGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = transcodetask.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Status transcodetask.Status `json:"status,omitempty"`
//	}
//
//	client.TranscodeTask.Query().
//		Select(transcodetask.FieldStatus).
//		Scan(ctx, &v)
func (_q *TranscodeTaskQuery) Select(fields ...string) *TranscodeTaskSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &TranscodeTaskSelect{TranscodeTaskQuery: _q}
	sbuild.label = transcodetask.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TranscodeTaskSelect configured with the given aggregations.
func (_q *TranscodeTaskQuery) Aggregate(fns ...AggregateFunc) *TranscodeTaskSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *TranscodeTaskQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !transcodetask.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *TranscodeTaskQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TranscodeTask, error) {
	var (
		nodes       = []*TranscodeTask{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withSourceArtifact != nil,
			_q.withArchiveArtifact != nil,
		}
	)
	if _q.withSourceArtifact != nil || _q.withArchiveArtifact != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, transcodetask.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TranscodeTask).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TranscodeTask{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withSourceArtifact; query != nil {
		if err := _q.loadSourceArtifact(ctx, query, nodes, nil,
			func(n *TranscodeTask, e *MediaArtifact) { n.Edges.SourceArtifact = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withArchiveArtifact; query != nil {
		if err := _q.loadArchiveArtifact(ctx, query, nodes, nil,
			func(n *TranscodeTask, e *MediaArtifact) { n.Edges.ArchiveArtifact = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *TranscodeTaskQuery) loadSourceArtifact(ctx context.Context, query *MediaArtifactQuery, nodes []*TranscodeTask, init func(*TranscodeTask), assign func(*TranscodeTask, *MediaArtifact)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TranscodeTask)
	for i := range nodes {
		if nodes[i].media_artifact_source_transcode_task == nil {
			continue
		}
		fk := *nodes[i].media_artifact_source_transcode_task
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(mediaartifact.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "media_artifact_source_transcode_task" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *TranscodeTaskQuery) loadArchiveArtifact(ctx context.Context, query *MediaArtifactQuery, nodes []*TranscodeTask, init func(*TranscodeTask), assign func(*TranscodeTask, *MediaArtifact)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*TranscodeTask)
	for i := range nodes {
		if nodes[i].media_artifact_archive_transcode_task == nil {
			continue
		}
		fk := *nodes[i].media_artifact_archive_transcode_task
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(mediaartifact.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "media_artifact_archive_transcode_task" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *TranscodeTaskQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *TranscodeTaskQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(transcodetask.Table, transcodetask.Columns, sqlgraph.NewFieldSpec(transcodetask.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, transcodetask.FieldID)
		for i := range fields {
			if fields[i] != transcodetask.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *TranscodeTaskQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(transcodetask.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = transcodetask.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TranscodeTaskGroupBy is the group-by builder for TranscodeTask entities.
type TranscodeTaskGroupBy struct {
	selector
	build *TranscodeTaskQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *TranscodeTaskGroupBy) Aggregate(fns ...AggregateFunc) *TranscodeTaskGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *TranscodeTaskGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TranscodeTaskQuery, *TranscodeTaskGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *TranscodeTaskGroupBy) sqlScan(ctx context.Context, root *TranscodeTaskQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TranscodeTaskSelect is the builder for selecting fields of TranscodeTask entities.
type TranscodeTaskSelect struct {
	*TranscodeTaskQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *TranscodeTaskSelect) Aggregate(fns ...AggregateFunc) *TranscodeTaskSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *TranscodeTaskSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TranscodeTaskQuery, *TranscodeTaskSelect](ctx, _s.TranscodeTaskQuery, _s, _s.inters, v)
}

func (_s *TranscodeTaskSelect) sqlScan(ctx context.Context, root *TranscodeTaskQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
